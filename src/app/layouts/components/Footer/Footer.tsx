import { AppShell } from "@mantine/core";

const Footer = () => {
  return (
    <AppShell.Footer style={{ position: "static", bottom: 0 }}>
      Footer
    </AppShell.Footer>
  );
};

export default Footer;
